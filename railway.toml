
# --- VoiceGuide AirLink — railway.toml ---
[build]
builder = "Docker"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/api/health"
healthcheckTimeout = 100

[service]
numReplicas = 1
restartPolicyType = "ON_FAILURE"
