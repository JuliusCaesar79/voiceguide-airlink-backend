# railway.toml

[service]
name = "voiceguide-airlink-backend"

[build]
# Usa il Dockerfile del repo
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# facoltative ma utili:
healthcheckPath = "/api/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
