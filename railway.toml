# ---- Railway app config ----
[build]
builder = "Dockerfile"

[deploy]
# Your API health endpoint (already implemented)
healthcheckPath = "/api/healthz"
healthcheckTimeout = 180
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
